# Worldality public examples
 
This repository is generated from the Worldality development monorepo. Treat
the runnable examples as framework-specific reference integrations for the
published `worldality` package.
 
- Keep examples compatible with the published package version in each package.json.
- Preserve the framework's existing router and SSR conventions.
- Use `$worldality` for installation, configuration, locale routing,
  metadata, and runtime troubleshooting guidance.
- Do not add private development paths, credentials, or internal service details.
